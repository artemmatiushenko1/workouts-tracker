export class WorkoutsView {
  #workoutsContainer = document.querySelector('.');

  render(workouts) {
    //workouts rendering logic
  }

  createWorkoutTemplate(data) {
    return 'html markup for one workout card';
  }
}
